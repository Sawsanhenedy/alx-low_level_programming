#include"lists.h"
/**
 * free_listint_safe - frees a listint_t
 * @h: pointer
 * Return: node's number
 */
size_t free_listint_safe(listint_t **h)
{
listint_t *nx, *ln;
size_t length;
int lp = 1;
if (h == NULL || *h == NULL)
return (0);
ln = i^
